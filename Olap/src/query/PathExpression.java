/**
 * 
 */
package query;

/**
 * Represent an Olap path
 *
 * The path expression represent an Olap path.
 * It is a tree, its leaf are FunctionReference and its root is a FunctionReference or an Operator
 */
public interface PathExpression {	
	
	
}
